name: mcpapps_bridge
description: Dart <-> MCP host bridge for MCP Apps Flutter components.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
